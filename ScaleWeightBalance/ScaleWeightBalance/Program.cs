using System;
using System.Text;

namespace ScaleWeightBalance
{
    class Program
    {
        static void Main(string[] args)

        {
            string[] arr = new string[] { "[5, 9]", "[1, 2, 6, 7]" };
            Console.WriteLine(ScaleBalance(arr));
        }
        public static string  ScaleBalance(string [] A)
        {
            StringBuilder builder = new StringBuilder();
          
            for(int i=0;i<A.Length; i++)
            {
                builder.Append(A[i]);
                
            }
            if (store != large)
            {
                continue;
            }
            else
            {
                IAsyncResult = values[i];
            }
           // string []LeftRight=builder[0];
            //string str= builder.ToString();
         

            
            //foreach (string value in A)
            //{
            //    builder.Append(value);
            //  //  builder.Append('.');
            //}
            Console.WriteLine(builder.ToString());
           // builder.ToString();

            return "djdjjd";
        }
    }
}
